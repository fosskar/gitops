{{- $firstNode := index .Values.nodes 0 }}
apiVersion: infrastructure.cluster.x-k8s.io/v1alpha1
kind: ProxmoxCluster
metadata:
  name: {{ .Values.clusterName }}
  namespace: {{ .Values.targetNamespace | default "default" }}
  annotations:
spec:
  credentialsRef:
    name: {{ .Values.proxmox.credentialsSecret }}
    namespace: {{ .Values.targetNamespace | default "default" }}
  allowedNodes:
    - {{ .Values.proxmox.node }}
  controlPlaneEndpoint:
{{- if eq $firstNode.type "single-node" }}
    host: {{ $firstNode.ip }}
{{- else }}
    host: {{ .Values.network.vip }}
{{- end }}
    port: 6443
  dnsServers:
    {{- range .Values.network.dnsServers }}
    - {{ . }}
    {{- end }}
  ipv4Config:
    addresses:
{{- if contains "," .Values.network.ipRange }}
{{- range (splitList "," .Values.network.ipRange) }}
      - {{ . }}
{{- end }}
{{- else }}
      - {{ .Values.network.ipRange }}
{{- end }}
    gateway: {{ .Values.network.gateway }}
    prefix: 24
  schedulerHints:
    memoryAdjustment: 0
