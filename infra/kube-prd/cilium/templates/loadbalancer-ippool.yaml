{{- if .Values.loadBalancerIPPool.enabled }}
apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  name: {{ .Values.loadBalancerIPPool.name }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  blocks:
  - cidr: {{ .Values.loadBalancerIPPool.cidr | quote }}
{{- end }}
