apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: sealed-secrets-qa
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: sealed-secrets-warehouse
      sources:
        direct: true
  promotionTemplate:
    spec:
      vars:
        - name: chartRepo
      steps:
        - task:
            name: infra-update-helm-dependency
            kind: PromotionTask
